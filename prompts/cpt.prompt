Extract the CPT codes from the medical record to identify which treatment has been recommended by the doctor. These appear at the end of the medical record after "Requested procedure:" and can be a single number or a comma separated list of numbers. You must return a valid JSON object using the following template and nothing else:

{"CPT": [LIST_OF_CPT_CODES]}